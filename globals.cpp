#include "globals.hpp"

namespace Globals {
    extern sf::SoundBuffer g_buffer = sf::SoundBuffer();
    extern sf::SoundBuffer g_bufferMenu = sf::SoundBuffer();
    extern sf::SoundBuffer g_bufferOut = sf::SoundBuffer();
}